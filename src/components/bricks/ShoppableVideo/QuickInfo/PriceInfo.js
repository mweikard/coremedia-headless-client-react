// @flow
import P from '../../../basic/Paragraph';
import withStyles from '../../../styles/withStyles';

const styles = {
  margin: '20px 10px 10px 2px',
  alignSelf: 'start',
};

export default withStyles(styles, 'ShoppableVideoQuickInfoPriceInfo')(P);
