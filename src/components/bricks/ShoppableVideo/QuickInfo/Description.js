// @flow
import P from '../../../basic/Paragraph';
import withStyles from '../../../styles/withStyles';

const styles = {
  padding: 0,
};

export default withStyles(styles, 'ShoppableVideoQuickInfoDescription')(P);
