// @flow
import { Title1 } from '../../../basic/Heading';
import withStyles from '../../../styles/withStyles';

const styles = {
  marginTop: '1em',
};

export default withStyles(styles, 'RichtextTitle1')(Title1);
