// @flow
import { Title4 } from '../../../basic/Heading';
import withStyles from '../../../styles/withStyles';

const styles = {
  marginTop: '0.25em',
};

export default withStyles(styles, 'RichtextTitle4')(Title4);
