// @flow
import { Title3 } from '../../../basic/Heading';
import withStyles from '../../../styles/withStyles';

const styles = {
  marginTop: '0.5em',
};

export default withStyles(styles, 'RichtextTitle3')(Title3);
