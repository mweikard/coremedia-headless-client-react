// @flow
import { Title2 } from '../../../basic/Heading';
import withStyles from '../../../styles/withStyles';

const styles = {
  marginTop: '0.75em',
};

export default withStyles(styles, 'RichtextTitle2')(Title2);
