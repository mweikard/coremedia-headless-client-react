// @flow
import { TeaserBox } from '../../../components/basic/Box';
import withStyles from '../../../components/styles/withStyles';

const styles = {
  border: '2px solid rgba(221, 52, 40, 1)',
};

export default withStyles(styles)(TeaserBox);
