// @flow
import createTheme from '../../components/styles/utils/createTheme';

import palette from './palette';
import typography from './typography';

const theme = createTheme({ palette, typography });

export default theme;
